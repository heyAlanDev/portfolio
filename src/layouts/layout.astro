---
import '@fontsource-variable/crimson-pro'
import '@fontsource-variable/archivo'

import Navbar from '@components/navbar.svelte'
import Footer from '@components/footer.astro'

interface Props {
  title: string
}

const { title } = Astro.props
---

<!doctype html>
<html lang="es" class="bg-black-400 font-sans">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="description"
      content="Impulso la innovación como un apasionado full-stack developer. Desde la creación de interfaces de usuario cautivadoras hasta el diseño de arquitecturas robustas en el back-end, mi enfoque integral ofrece soluciones digitales excepcionales. Explora mi portafolio para descubrir cómo puedo llevar tu proyecto al siguiente nivel. ¡Convierte tus ideas en experiencias digitales impactantes con mi experiencia en desarrollo full-stack!"
    />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title ?? 'Alan Valencia'}</title>
    <!-- Add polyfill for animation-timeline (scroll-timeline and view-timeline) -->
    <script is:inline src="https://flackr.github.io/scroll-timeline/dist/scroll-timeline.js"></script>
  </head>
  <body class="w-full h-full flex content-center flex-col justify-center transition-colors text-black-400 dark:text-slate-100">
    <Navbar client:load />
    <main>
      <slot />
    </main>
    <Footer id="#contact" />
  </body>
</html>

<style is:global>
  :root {
    --accent: 136, 58, 234;
    --accent-light: 224, 204, 250;
    --accent-dark: 49, 10, 101;
  }

  * {
    scrollbar-width: thin;
    scrollbar-color: #212121 #bebebe;
  }

  *::-webkit-scrollbar {
    width: 0.25rem;
    height: 0.25rem;
  }

  *::-webkit-scrollbar-track {
    background: rgb(179, 177, 177);
    border-radius: 10px;
  }

  *::-webkit-scrollbar-thumb {
    background: rgb(136, 136, 136);
    border-radius: 10px;
  }

  *::-webkit-scrollbar-thumb:hover {
    background: rgb(100, 100, 100);
    border-radius: 10px;
  }

  *::-webkit-scrollbar-thumb:active {
    background: rgb(68, 68, 68);
    border-radius: 10px;
  }

  .small-caps {
    font-variant-caps: small-caps;
  }
</style>
